spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/aiimage
    username: user_ai
    password: AMPy4MoYP2eHpvH83qFW
  jpa:
    hibernate:
      ddl-auto: update
  redis:
    host: ${REDIS_HOST:redis}
    port: ${REDIS_PORT:6379}

minio:
  url: http://${MINIO_HOST:minio}:9000
  access-key: ${MINIO_ROOT_USER}
  secret-key: ${MINIO_ROOT_PASSWORD}
  bucket: ${MINIO_BUCKET}

app:
  image:
    retention-years: ${IMAGE_RETENTION_YEARS}